function userReducer(state, action) {
    switch (action.type) {
        case "CREATE_ACCOUNT":
            break;
        case "LOGIN":
            break;
        default:
            return state;
    }
}

export default userReducer;
